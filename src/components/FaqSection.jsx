import { useState } from 'react';
import { Reveal, Section, SectionHeader, Pill, AccordionItem } from './shared';

const FAQ = [
  { q: 'Сколько времени занимает внедрение?', a: 'В среднем от 2 до 5 недель: от аудита процессов до запуска агента в работу. Сроки зависят от сложности задач и числа интеграций.' },
  { q: 'Нужно ли менять CRM и текущие системы?', a: 'Нет. Агент встраивается в ваши инструменты — AmoCRM, МойСклад, мессенджеры, базы знаний — без замены инфраструктуры.' },
  { q: 'Что с безопасностью данных?', a: 'Данные обрабатываются на серверах в РФ, соответствие 152-ФЗ, персональные данные скрываются до отправки в ИИ, доступы разграничены.' },
  { q: 'Что если ИИ-сервис упадёт?', a: 'Система автоматически переключается на резервную российскую модель и продолжает работать. Отказоустойчивость заложена в архитектуру.' },
  { q: 'Сколько это стоит?', a: 'Стоимость зависит от задач и объёма автоматизации. На бесплатном аудите считаем потенциальную экономию и стоимость внедрения под ваш бизнес.' },
  { q: 'Подойдёт ли это нашему бизнесу?', a: 'Если есть повторяющиеся процессы, переписки и обработка заявок — агент окупается. На аудите честно скажем, есть ли смысл внедрять.' },
];

export default function FaqSection() {
  const [open, setOpen] = useState(-1);
  return (
    <Section id="faq" className="py-20 md:py-28">
      <div className="grid gap-12 lg:grid-cols-[0.9fr_1.4fr]">
        <div>
          <SectionHeader
            index="08" kicker="Вопросы"
            title={<>Коротко <span className="text-orange">о главном</span></>}
            intro="Не нашли ответ? Задайте вопрос на бесплатном аудите."
          />
          <Reveal delay={160} className="mt-8 hidden lg:block">
            <Pill href="#audit" variant="dark" arrow>Получить бесплатный аудит</Pill>
          </Reveal>
        </div>
        <div className="grid gap-4">
          {FAQ.map((f, i) => (
            <Reveal key={f.q} delay={i * 60}>
              <AccordionItem q={f.q} a={f.a} idx={`faq${i}`} open={open === i} onToggle={() => setOpen(open === i ? -1 : i)} />
            </Reveal>
          ))}
        </div>
      </div>
    </Section>
  );
}
